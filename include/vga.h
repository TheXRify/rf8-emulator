#ifndef VGA_H
#define VGA_H

#define COLOR_BITS 8 // RR, GG, BB, AA (bits are in sequence and never out of order)
#define ADDRESS_BITS 8
#define DATA_BITS 12 // COLOR, XX, YY
#define MEMORY_SIZE 4096 // pixel data of the window

#endif // VGA_H